name: Lint

on:
  workflow_run:
    workflows: ["Black Formatter"]
    branches: ["main"]
    types: 
      - completed
  
jobs:
  flake8:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: pip install flake8
      - run: make lint
